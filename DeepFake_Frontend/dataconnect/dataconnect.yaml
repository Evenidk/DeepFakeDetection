specVersion: "v1beta"
serviceId: "my-next-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-next-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
